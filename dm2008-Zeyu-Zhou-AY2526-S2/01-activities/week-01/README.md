# Week01
First time learning about javascript but was because I have been working with c# in Unity, so the language is somewhat familiar and ideas are interchangable.
For example, setup() = start(), draw() = update()
However,somethings are still unfamilar. For example, I need to get used to using 0 point at the top left ocrner, and y-axis positive is going down.
Another thing is that variable has much lesser types. Unlike C# needing to define float, int, etc.
Another concept that I know but always forget is that animations on screens are always fake, what the computer doing is drawing the frames per second to create the illusion of movement, which Unity takes care with their built-in features.

## Activity 1A
Tried drawing a cat with the new p5js built-in functions.

## Activity 1B
This activity I tried to create some interactions.

1. Tested with black and white to spawn random width rectangles and random position with fixed height so the screen split into two sections.
2. Map the rectangle spawn height to mouseY.
3. Add color for the two sections.
4. Map the rectangle spawn area width to mouseX.
5. Add color to the newly added two sections.
