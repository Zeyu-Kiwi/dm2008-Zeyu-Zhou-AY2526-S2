# Week03
This week we started learning array, which is a great variable to store multiple data in. You can use push() and splice() to edit array's length. Which in Unity only List can have dynamic length, array length is fixed.

Then also introduced function, passing and returning value with functions. Which I often forget function can pass and return, I usally just use them as a container to store codes and call it when needed.

We also started learning transforms, which took me a while to get used to translate(). I was so used to Unity rotating from the centre and was so puzzle with the shapes rotating from the top left corner, and actaully rotating the whole canvas instead of just the shape itself...

## Activity 3A
This activity was just trying out array and push() and splice() funciton. Press E will add a random size and color circle into the array, if array length > then x, remove the last index. Lastly, pressing left mouse button will loop through the array.

## Activity 3B
This is the activity that was a headache for me, the one that I learn about rotation and translate... I was just trying to rotate the bird from the centre and it took me so long to figure out...

## Activity 3C
This activity was to make use of all the things we learnt to create a simple drawing tool.
